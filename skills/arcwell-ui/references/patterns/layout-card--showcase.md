<!-- Generated by scripts/generate-llm-guidance.mjs. Edit component/Storybook sources, then run npm run skills:generate. -->

# Layout/Card / Showcase

The showcase layout composes a configuration rail, live preview, usage guidance, and copyable code. It collapses into one column on mobile.

## Evidence and components

- [Storybook](http://127.0.0.1:6006/?path=/story/layout-card--showcase)
- [Exact CSF source](../../../../projects/design-library/src/lib/card.stories.ts)
- [ButtonComponent](../components/button.md)
- [CardComponent](../components/card.md)
- [CodeBlockComponent](../components/code-block.md)

## Overrides

```ts
{ layout: "showcase", showHeader: false }
```

## Render recipe

Resolve helper constants, imported components and inherited args from the CSF source. This excerpt is not a standalone component.



```ts
(args) => ({
    props: args,
    template: `<dl-card ${argsToTemplate(args)}>
      <div cardRail>
        <strong style="display:block;margin-bottom:8px">variant</strong>
        <p style="margin:0 0 18px">Choose the hierarchy that fits the action.</p>
        <div style="display:grid;gap:8px">
          <button dlButton variant="secondary" fullWidth>Primary</button>
          <button dlButton variant="tertiary" fullWidth>Secondary</button>
          <button dlButton variant="tertiary" fullWidth>Tertiary</button>
        </div>
      </div>
      <div cardPreview><button dlButton>Continue</button></div>
      <div cardGuidance><small>WHEN TO USE PRIMARY</small><p>Use once per region for the action most likely to move the task forward.</p></div>
      <dl-code-block cardCode language="Angular" [code]="codeSample" />
      <span cardFooter>Optional footer content.</span>
    </dl-card>`,
  })
```

## Adaptation prompt

Build **[requested feature]** using this composition as a starting point. Read the linked component APIs and original Storybook setup. Preserve the requested layout, form value types, projection selectors and accessible controls; adapt the content and handlers to the application. Add only the states needed for the task. Verify relevant interactions, responsive widths and Actions logging. The example demonstrates a possible composition, not a mandate to reproduce its copy, data or styling.

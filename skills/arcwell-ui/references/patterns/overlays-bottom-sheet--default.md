<!-- Generated by scripts/generate-llm-guidance.mjs. Edit component/Storybook sources, then run npm run skills:generate. -->

# Overlays/Bottom sheet / Default

A composition detected from an existing explicit Storybook render recipe.

## Evidence and components

- [Storybook](http://127.0.0.1:6006/?path=/story/overlays-bottom-sheet--default)
- [Exact CSF source](../../../../projects/design-library/src/lib/overlays/bottom-sheet.stories.ts)
- [ButtonComponent](../components/button.md)
- [InputComponent](../components/input.md)
- [BottomSheetComponent](../components/bottom-sheet.md)

## Overrides

```ts
{
    open: false,
    heading: "Bottom sheet",
    description: "A flexible space for focused work.",
    confirmLabel: "Save changes",
    confirmVariant: "primary",
    autoFocus: "first-tabbable",
  }
```

## Render recipe

Resolve helper constants, imported components and inherited args from the CSF source. This excerpt is not a standalone component.



```ts
(args, context) => ({
    props: { ...args, id: `overlay-${context.id}`, result: "" },
    template: `<button dlButton (click)="$any($event.currentTarget).focus();open=true">Open bottom sheet</button><dl-bottom-sheet ${argsToTemplate(args, { exclude: ["openChange", "closed"] })} (openChange)="open=$event;openChange && openChange($event)" (closed)="result=$event;closed && closed($event)"><p>Use any components, forms, or content here.</p><dl-input [id]="id+'-workspace'" label="Workspace name" ngModel="Studio"/></dl-bottom-sheet><p style="font:12px var(--dl-font);color:var(--dl-muted)">Closed: <output>{{result}}</output></p>`,
  })
```

## Adaptation prompt

Build **[requested feature]** using this composition as a starting point. Read the linked component APIs and original Storybook setup. Preserve the requested layout, form value types, projection selectors and accessible controls; adapt the content and handlers to the application. Add only the states needed for the task. Verify relevant interactions, responsive widths and Actions logging. The example demonstrates a possible composition, not a mandate to reproduce its copy, data or styling.

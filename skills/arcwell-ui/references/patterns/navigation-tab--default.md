<!-- Generated by scripts/generate-llm-guidance.mjs. Edit component/Storybook sources, then run npm run skills:generate. -->

# Navigation/Tab / Default

A composition detected from an existing explicit Storybook render recipe.

## Evidence and components

- [Storybook](http://127.0.0.1:6006/?path=/story/navigation-tab--default)
- [Exact CSF source](../../../../projects/design-library/src/lib/navigation/tab.stories.ts)
- [InputComponent](../components/input.md)
- [TabContainerComponent](../components/tab-container.md)
- [TabComponent](../components/tab.md)

## Overrides

```ts
{ value: "overview", label: "Overview", closable: false }
```

## Render recipe

Resolve helper constants, imported components and inherited args from the CSF source. This excerpt is not a standalone component.



```ts
(args, context) => ({
    props: { ...args, showFirst: true },
    template: `<dl-tab-container label="Project tabs">@if(showFirst){<dl-tab ${argsToTemplate(args)} (closed)="showFirst=false"><h3>Project overview</h3><dl-input id="${context.id}-project" label="Project name" ngModel="Studio"/></dl-tab>}<dl-tab value="activity" label="Activity" badge="3"><h3>Recent activity</h3><p>Your team is up to date.</p></dl-tab><dl-tab value="settings" label="Settings" [disabled]="true">Restricted settings</dl-tab></dl-tab-container>`,
  })
```

## Adaptation prompt

Build **[requested feature]** using this composition as a starting point. Read the linked component APIs and original Storybook setup. Preserve the requested layout, form value types, projection selectors and accessible controls; adapt the content and handlers to the application. Add only the states needed for the task. Verify relevant interactions, responsive widths and Actions logging. The example demonstrates a possible composition, not a mandate to reproduce its copy, data or styling.

<!-- Generated by scripts/generate-llm-guidance.mjs. Edit component/Storybook sources, then run npm run skills:generate. -->

# Actions/Button / Variants

Primary is filled, secondary is outlined with a surface background, and tertiary is transparent. The same three variants work on standard buttons, icon buttons and floating action buttons. Click events appear in Actions.

## Evidence and components

- [Storybook](http://127.0.0.1:6006/?path=/story/actions-button--variants)
- [Exact CSF source](../../../../projects/design-library/src/lib/button.stories.ts)
- [ButtonComponent](../components/button.md)
- [FabButtonComponent](../components/fab-button.md)
- [IconButtonComponent](../components/icon-button.md)

## Overrides

```ts
Inherits the component story defaults.
```

## Render recipe

Resolve helper constants, imported components and inherited args from the CSF source. This excerpt is not a standalone component.



```ts
() => ({
    props: { variants: ["primary", "secondary", "tertiary"] },
    template: `<div style="display:flex;flex-wrap:wrap;gap:32px">@for(v of variants;track v){<section style="display:flex;flex-direction:column;align-items:flex-start;gap:16px"><h3 style="margin:0;text-transform:capitalize">{{v}}</h3><button dlButton [variant]="v">Continue</button><button dlIconButton [variant]="v" icon="plus" [label]="v+' icon button'"></button><button dlFab [variant]="v" icon="plus" [label]="v+' floating action'"></button></section>}</div>`,
  })
```

## Adaptation prompt

Build **[requested feature]** using this composition as a starting point. Read the linked component APIs and original Storybook setup. Preserve the requested layout, form value types, projection selectors and accessible controls; adapt the content and handlers to the application. Add only the states needed for the task. Verify relevant interactions, responsive widths and Actions logging. The example demonstrates a possible composition, not a mandate to reproduce its copy, data or styling.

<!-- Generated by scripts/generate-llm-guidance.mjs. Edit component/Storybook sources, then run npm run skills:generate. -->

# Navigation/Carousel directive / Custom Pagination

Build your own pagination with the exported directive. goTo(index), next(), previous(), pause() and play() work with the library’s buttons or your own controls.

## Evidence and components

- [Storybook](http://127.0.0.1:6006/?path=/story/navigation-carousel-directive--custom-pagination)
- [Exact CSF source](../../../../projects/design-library/src/lib/navigation/carousel.stories.ts)
- [ButtonComponent](../components/button.md)
- [CardComponent](../components/card.md)
- [CarouselDirective](../components/carousel.md)

## Overrides

```ts
Inherits the component story defaults.
```

## Render recipe

Resolve helper constants, imported components and inherited args from the CSF source. This excerpt is not a standalone component.

```ts
const cards = `@for (item of items; track item) {<dl-card [heading]="'Project '+item" description="A reusable card inside the carousel."><p>Compose your own content and actions.</p><button dlButton variant="secondary">Open project {{item}}</button></dl-card>}`;
```

```ts
(args) => ({
    props: { ...args, items: [1, 2, 3, 4, 5] },
    template: `<div style="width:min(760px,100%);min-width:0"><div #carousel="dlCarousel" ${argsToTemplate(args)}>${cards}</div><nav aria-label="Carousel pagination" style="display:flex;gap:8px;flex-wrap:wrap;margin-top:20px">@for(item of items;track item;let i=$index){<button dlButton [variant]="carousel.index()===i?'primary':'secondary'" [attr.aria-current]="carousel.index()===i?'true':null" [attr.aria-label]="'Go to slide '+item" (click)="carousel.goTo(i)">{{item}}</button>}</nav></div>`,
  })
```

## Adaptation prompt

Build **[requested feature]** using this composition as a starting point. Read the linked component APIs and original Storybook setup. Preserve the requested layout, form value types, projection selectors and accessible controls; adapt the content and handlers to the application. Add only the states needed for the task. Verify relevant interactions, responsive widths and Actions logging. The example demonstrates a possible composition, not a mandate to reproduce its copy, data or styling.

<!-- Generated by scripts/generate-llm-guidance.mjs. Edit component/Storybook sources, then run npm run skills:generate. -->

# Inputs/Number input / Reusable Validators

Exported numericValidator, currencyValidator, percentageValidator and precisionValidator work independently with Angular FormControls. Here currencyValidator is installed on the control and dlValidation displays its errors.

## Evidence and components

- [Storybook](http://127.0.0.1:6006/?path=/story/inputs-number-input--reusable-validators)
- [Exact CSF source](../../../../projects/design-library/src/lib/inputs/number-input.stories.ts)
- [NumberInputComponent](../components/number-input.md)
- [ValidationDirective](../components/validation.md)

## Overrides

```ts
{ validatePrecision: false }
```

## Render recipe

Resolve helper constants, imported components and inherited args from the CSF source. This excerpt is not a standalone component.



```ts
(args) => ({
    props: {
      ...args,
      control: new FormControl(12.345, {
        validators: [
          Validators.required,
          currencyValidator({ min: 0, max: 100 }),
        ],
      }),
    },
    template: `<div style="min-height:300px"><dl-number-input ${argsToTemplate(args)} [formControl]="control" dlValidation validationDisplay="inline" validationWhen="always"/><p>Form status: {{control.status}}</p></div>`,
  })
```

## Adaptation prompt

Build **[requested feature]** using this composition as a starting point. Read the linked component APIs and original Storybook setup. Preserve the requested layout, form value types, projection selectors and accessible controls; adapt the content and handlers to the application. Add only the states needed for the task. Verify relevant interactions, responsive widths and Actions logging. The example demonstrates a possible composition, not a mandate to reproduce its copy, data or styling.

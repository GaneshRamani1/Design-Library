const guidance = {
  AlertComponent: ["Communicate an important inline status or required action.", "Use alerts beside the content they explain; choose the live-region role from urgency."],
  AvatarComponent: ["Represent a person or entity with an image or deterministic initials.", "Always provide a meaningful name and test image failure and compact layouts."],
  BadgeComponent: ["Show a short status, category, or count beside related content.", "Keep badge text brief and ensure custom colors retain readable contrast."],
  ButtonComponent: ["Trigger an immediate action with a clear visual priority.", "Use one primary action per region and expose loading and disabled behavior."],
  CardComponent: ["Group related content and actions into a responsive surface.", "Choose headingLevel from the surrounding document hierarchy and use projection for rich content."],
  InputComponent: ["Collect a single text value with labeling, hints, validation, and optional masking.", "Preserve a stable raw/form value contract and explain format requirements before validation fails."],
  ProgressComponent: ["Communicate determinate or indeterminate task progress.", "Supply an accessible value label and avoid animated progress when reduced motion is requested."],
  ToggleComponent: ["Change a setting whose effect is immediate.", "Use a checkbox when the value is submitted later with a form."],
  FabButtonComponent: ["Expose a prominent contextual action over application content.", "Respect mobile safe areas and avoid obscuring navigation or primary content."],
  IconButtonComponent: ["Trigger a compact action represented by an icon.", "A visible tooltip may help sighted users, but label remains required for accessibility."],
  DividerComponent: ["Separate adjacent content regions visually or semantically.", "Use decorative mode when the separator does not represent a document boundary."],
  ListComponent: ["Present repeated records with optional interaction and selection.", "Choose explicit keyboard and selection behavior when items act like controls."],
  TilesComponent: ["Summarize dashboard metrics, trends, progress, and actions.", "Keep the primary value understandable without relying on the miniature chart or color."],
  GlobalNotificationComponent: ["Announce application-wide information independent of page content.", "Define stacking, persistence, and dismissal behavior at application level."],
  InlineNotificationComponent: ["Explain a status within the content region it affects.", "Place it close to the relevant control or workflow and match live-region urgency to the message."],
  SkeletonComponent: ["Reserve content shape while data is loading.", "Mark the owning region busy and disable animation for reduced-motion users."],
  SnackbarComponent: ["Confirm a short-lived action, optionally with one recovery action.", "Use concise copy and give undo actions enough time to operate."],
  ToastComponent: ["Show transient, nonblocking feedback above application content.", "Queue and deduplicate repeated messages and keep critical errors persistent."],
  IconComponent: ["Render a registered or custom SVG icon consistently.", "Use decorative icons without labels and give standalone meaningful icons an accessible label."],
  CheckboxComponent: ["Select one or more independent options.", "Use indeterminate only to represent a real mixed child-selection state."],
  ChipsComponent: ["Display compact values that may be selected or removed.", "Keep remove controls keyboard accessible and announce the affected item."],
  ContextSelectorComponent: ["Switch the active workspace, account, environment, or similar context.", "Make the scope change explicit because it can affect the entire page."],
  CounterButtonComponent: ["Adjust a numeric value through explicit increment and decrement actions.", "Communicate limits and preserve precision consistently between display and form value."],
  CounterComponent: ["Display and optionally edit a bounded count.", "Use clear units and prevent buttons from implying that an unavailable value can be selected."],
  DatepickerComponent: ["Select a calendar date through typing or a popup calendar.", "Keep parsing, locale, min/max, and disabled-date rules consistent across both input methods."],
  DropdownComponent: ["Choose one option from a compact list.", "Use searchable or virtualized patterns when the option set becomes long or remote."],
  MultiSelectComponent: ["Choose multiple options with search and bulk actions.", "Define whether Select all applies to visible results or the entire dataset."],
  NumberInputComponent: ["Edit a number while presenting locale-aware numeric, currency, or percentage formatting.", "Document raw value, rounding, precision, and edit-time formatting as one stable contract."],
  RadioComponent: ["Choose exactly one option from a visible set.", "Present a group label and keep arrow-key navigation within the option group."],
  RangeSelectorComponent: ["Select one value or a bounded interval on a numeric scale.", "Expose both handles and formatted values to keyboard and assistive-technology users."],
  SegmentedButtonsComponent: ["Choose among a small set of closely related modes or values.", "Use tabs when selection changes the visible content panel rather than a setting."],
  ValidationDirective: ["Apply reusable synchronous or asynchronous validation and display its messages inline or in a popover.", "Use typed error keys, cancel obsolete async work, and show pending state when validation is not immediate."],
  ContainerComponent: ["Constrain and arrange page content with shared responsive spacing.", "Prefer token-backed sizes and allow children to wrap before narrow layouts overflow."],
  HeaderComponent: ["Compose a page or section heading with context, metadata, and projected actions.", "Preserve heading hierarchy and let metadata and actions wrap predictably on mobile."],
  PaneComponent: ["Create a bounded layout region with explicit sizing and overflow.", "Set minimum sizes for flex children and verify scrolling with long content."],
  SectionComponent: ["Create a labeled semantic region with consistent responsive spacing.", "Associate the section landmark with its visible heading."],
  BreadcrumbComponent: ["Show the current location in a navigation hierarchy.", "Collapse intermediate items on narrow screens while retaining an accessible path."],
  CarouselDirective: ["Add paged scrolling behavior to suitable projected components.", "Support keyboard, touch, reduced motion, responsive slide counts, and useful slide announcements."],
  LinkDirective: ["Navigate to a destination using consistent link styling and semantics.", "Use native links or router destinations and identify new-window or download behavior in the accessible name."],
  StepComponent: ["Describe one state in a multi-step workflow.", "Expose current, completed, invalid, optional, and disabled states semantically."],
  StepperContainerComponent: ["Coordinate step navigation, panels, and linear workflow rules.", "Validate before advancing and preserve entered data when users move between steps."],
  TabContainerComponent: ["Coordinate tabs, keyboard focus, selection, and associated panels.", "Use route synchronization for navigable views and an overflow strategy for narrow screens."],
  TabComponent: ["Define one selectable tab and its associated projected panel.", "Keep labels concise and make closing an active tab deterministic."],
  BottomSheetComponent: ["Present a mobile-oriented modal surface from the bottom edge.", "Open at 90% height by default, respect safe areas, and retain a clear dismissal path."],
  ConfirmationDialogComponent: ["Request explicit confirmation before a consequential operation.", "Prevent repeat submission and expose pending and failure states for asynchronous work."],
  ModalComponent: ["Focus attention on a bounded task above the current page.", "Move focus into the dialog, trap it while open, and restore it to the trigger on close."],
  PopoverDirective: ["Open interactive contextual content beside a trigger.", "Anchor to the trigger element, handle viewport collisions, and restore focus after keyboard dismissal."],
  SidepanelComponent: ["Present supporting detail or a workflow from a viewport edge.", "Use full-screen takeover on narrow devices and guard unsaved work before closing."],
  TooltipDirective: ["Provide short, noninteractive clarification for a specific trigger.", "Position from the trigger, support focus and hover, and never put required information only in a tooltip."],
};

export function guidanceFor(component, fallback) {
  const value = guidance[component];
  return value ?? [`Configure and compose ${fallback} with the public API.`, "Verify semantics, keyboard behavior, long content, and narrow layouts before release."];
}

export function outputExplanation(name) {
  const exact = {
    valueChange: "Emits the new public value after a user or controlled selection change.",
    openChange: "Emits the controlled open state whenever the overlay opens or closes.",
    visibleChange: "Emits the controlled visibility state after display or dismissal.",
    dismissed: "Emits when the user dismisses the surface, including its dismissal context when available.",
    action: "Emits when the component's optional action is activated.",
    confirmed: "Emits when the confirm action is activated.",
    cancelled: "Emits when the cancel action is activated.",
    closed: "Emits after the component closes; inspect the payload for its close reason.",
    shown: "Emits after anchored content becomes visible.",
    hidden: "Emits after anchored content is dismissed.",
    selected: "Emits the selected navigation item.",
    expanded: "Emits when collapsed navigation items are revealed.",
    slideChange: "Emits the active slide state after carousel navigation settles.",
    indexChange: "Emits the controlled zero-based carousel index.",
    validationChange: "Emits the current validation status and messages when validation changes.",
    maskAccept: "Emits after the mask accepts an edit, even when the value is incomplete.",
    maskComplete: "Emits when every required position in the active mask is complete.",
    inputRejected: "Emits when numeric text cannot be accepted under the active formatter or bounds.",
    formattedChange: "Emits the display string produced by the numeric formatter.",
  };
  return exact[name] ?? `Emits the ${name.replace(/([a-z])([A-Z])/g, "$1 $2").toLowerCase()} interaction payload.`;
}
